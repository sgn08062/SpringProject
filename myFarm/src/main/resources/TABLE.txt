CREATE TABLE SHOP_ITEM_TABLE (
    ITEM_ID     BIGINT          NOT NULL AUTO_INCREMENT PRIMARY KEY,
    ITEM_NAME   VARCHAR(100)    NOT NULL,
    PRICE       INT             NOT NULL,
    STOR_ID     VARCHAR(15)     NULL,      -- 재고/저장 테이블의 FK
    REG_DATE    DATETIME        DEFAULT NOW()
);