package com.example.myFarm.user;

//import com.example.myFarm.command.DummyVO;
import com.example.myFarm.command.ItemVO;
import java.util.List;

public interface DummyService {
    /*
    // ✅ 유지: 상품 조회 기능 (UserController가 임시로 사용)
    List<ItemVO> getAllShopItems();
    List<ItemVO> searchAndSortShopItems(String searchKeyword, String sortField);
    ItemVO getShopItemDetail(Long itemId);

    // ✅ 유지: 미구현된 다른 더미 기능
    DummyVO getProductDetail(Long prodId);
    List<ItemVO> getOrderItems(Long orderId);
    List<ItemVO> getCheckoutItems(int userId);
    int getInventoryAmount(String cropUuid);
    int updateInventory(String cropUuid, int newAmount);

    // ❌ 제거/주석 처리: User/Address 관련 메서드 (AccountService/UserService로 분리 완료)
    // DummyVO getUserInfo(String loginId);
    // int addAddress(AddressVO address);

     */
}